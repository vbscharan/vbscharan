#include <stdio.h>
int main()
{
    int n1,n2;
    printf("Enter size of n1,n2:\n");
    scanf("%d %d",&n1,&n2);
    int a[n1],b[n2],c[n1+n2];
    printf("Enter values to the first array:\n");
    for(int i=0;i<n1;i++){
        scanf("%d",&a[i]);
    }
    printf("Enter values to the second array:\n");
    for(int i=0;i<n2;i++){
        scanf("%d",&b[i]);
    }
    printf("\n values to the first array:\n");
    for(int i=0;i<n1;i++){
        printf("%d ",a[i]);
    }
    printf(" \nvalues to the second array:\n");
    for(int i=0;i<n2;i++){
        printf("%d ",b[i]);
    }
    printf("\nSorting arrays\n");
    for(int i=0,j=0,k=0;(i<(n1+n2) && (j<n1 || k<n2));i++)
    {
        if(a[j]<=b[k])
        {
            c[i]=a[j];
            j++;
        }
        else 
        {
            c[i]=b[k];
            k++;
        }
